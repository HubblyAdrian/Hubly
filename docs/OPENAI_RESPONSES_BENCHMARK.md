# OpenAI transport benchmark — Chat Completions vs Responses

Generated by `scripts/benchmark-openai-transport.mjs`.

**Purpose:** Compare latency, token usage, failure rate, and JSON reliability for HublyAI’s OpenAI provider before production cutover to Responses.

## How to run

```bash
OPENAI_API_KEY=sk-… node scripts/benchmark-openai-transport.mjs
# optional:
# OPENAI_MODEL=gpt-5.5
# BENCHMARK_RUNS=3
```

Writes this file and prints a summary.

## Results (latest run)

| Metric | Chat Completions | Responses |
|---|---:|---:|
| Runs | — | — |
| Avg latency (ms) | — | — |
| Failure rate | — | — |
| JSON parse success | — | — |
| Avg input tokens | — | — |
| Avg output tokens | — | — |
| Avg total tokens | — | — |

> **Note (this environment):** Live OpenAI keys were not available in the cloud agent. Structural regression (`scripts/check-openai-responses.mjs`) passed. Re-run the benchmark with a production/staging `OPENAI_API_KEY` before merge and paste numbers above.

## Capability smoke (manual / staging)

After transport switch, confirm each HublyAI façade still returns usable text/JSON with Memory + DNA injected:

| Capability | Entry | Pass? |
|---|---|---|
| Business Build | `Hubly.buildBusiness` / `hubly-build-business` | ☐ |
| Website Runtime | `generateWebsite` / `runWebsite` | ☐ |
| Creative Director | `creativeDirector` (+ image) | ☐ |
| Storefront Chat | `customerConcierge` | ☐ |
| Draft Customer Message | `customerSupport` | ☐ |
| Photo Analysis | `photoAnalysis` | ☐ |
| Import Offers | `complete` / photo_analysis | ☐ |
| Marketplace Intake | `customerConcierge` | ☐ |
| Ask AI | `businessCoach` | ☐ |

## Rollback check

1. Set `OPENAI_TRANSPORT=chat` on the Edge runtime.
2. Confirm logs show `HublyAI.openai.transport … transport: "chat"`.
3. Re-hit Ask AI or storefront chat — no product code change required.
4. Set `OPENAI_TRANSPORT=responses` (or unset) to restore.
